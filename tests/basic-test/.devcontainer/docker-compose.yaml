services:
  basic-test-dev:
    container_name: basic-test-dev
    image: localhost:5000/basic-test-devcontainer:latest
    command: /bin/sh -c "sleep infinity"
    volumes:
      - ../:/src
      - ../tests/basic-test/logs:/logs
    env_file:
      - .env
